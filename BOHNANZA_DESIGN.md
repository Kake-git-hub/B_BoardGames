# ボーナンザ（Bohnanza）実装設計書

対面プレイ補助アプリ B_BoardGames に追加する「ボーナンザ」の設計。
本アプリの思想（全員が同じ場に集まり、交渉・会話は口頭で行い、アプリは「カード・場・コインの管理」だけを肩代わりする）に合わせたデジタル化。

- 対応人数: **3〜5人**（基本ゲーム準拠。2人変則ルール・6〜7人拡張は将来課題。6人以上では開始ボタンでエラー表示）
- DBパス: `bohnanzaRooms/<roomId>`（**Firebase Console のルール追記が必要** — README 7.3 参照）
- kind: `bohnanza` / 関数プレフィックス: `bz` / CSSプレフィックス: `bz-`

---

## 1) ゲームルール（基本版準拠）

### 1.1 カード構成（全104枚・8種）

| key | 表示名 | 枚数 | 豆メーター（1金/2金/3金/4金に必要な枚数） | 色 | 絵文字 |
|---|---|---|---|---|---|
| blue | あおまめ | 20 | 4 / 6 / 8 / 10 | #3b82f6 | 🔵 |
| chili | チリまめ | 18 | 3 / 6 / 8 / 9 | #ef4444 | 🌶️ |
| stink | そらまめ | 16 | 3 / 5 / 7 / 8 | #a16207 | 🫛 |
| green | みどりまめ | 14 | 3 / 5 / 6 / 7 | #22c55e | 🟢 |
| soy | だいず | 12 | 2 / 4 / 6 / 7 | #eab308 | 🌱 |
| blackeye | くろめまめ | 10 | 2 / 4 / 5 / 6 | #6b7280 | 👁️ |
| red | あかまめ | 8 | 2 / 3 / 4 / 5 | #dc2626 | ❤️ |
| garden | にわまめ | 6 | − / 2 / 3 / − | #16a34a | 🪴 |

- にわまめは「1金」「4金」の段が存在しない（2枚で2金、3枚以上で3金が上限。1枚で収穫すると0金）。
- 豆メーターの読み方: 収穫時、畑の枚数 ≧ しきい値となる最大の金額をもらう。どのしきい値にも届かなければ0金。
- 表示名は仮称。スキャンデータ格納時に公式名へ変更可。

### 1.2 進行（手番プレイヤーのフェーズ）

1. **うえる（plant）**: 手札の**先頭**の1枚を必ず植える（手札が0枚なら省略可）。続けて（新たな）先頭の1枚を植えてもよい（最大2枚）。植え終えたら手番プレイヤーが「2まい めくる」を押してフェーズ2へ。
2. **めくって こうかん（trade）**: 山札から2枚を表向きにめくる。手番プレイヤーは、めくれた2枚と自分の手札の任意のカードを使って、他プレイヤーと交渉・交換・譲渡できる。他プレイヤーは手番プレイヤーとしか交換できない。**交換・譲渡で得たカードは手札に入らず、必ず植える**。手番プレイヤーが「こうかん おわり」を押すとフェーズ3へ（めくれ残りは手番プレイヤーの植える待ちへ）。
3. **もらった豆を うえる（plantAll）**: 植える待ち（pending）を持つ全員が植える。全員の pending が空になったら自動でフェーズ4へ。
4. **ひく（draw・自動処理）**: 手番プレイヤーは山札から3枚を1枚ずつ引き、**手札の一番うしろ**に加える。次のプレイヤーの plant フェーズへ。

### 1.3 大原則

- **手札の並び替え禁止**: 手札は常に入手順の列。植えられるのは先頭のみ。引いたカードは末尾へ。
- **畑**: 各自2つ（**3人プレイ時は最初から3つ**）。1つの畑には1種類の豆のみ。同種は枚数を重ねる。
- **3つめの畑**: 4〜5人プレイでは、いつでも3金を支払って購入できる（支払ったコインはゲームから除外）。
- **収穫はいつでも**: 誰の手番中・どのフェーズ中でも、自分の畑を選んで収穫できる。豆メーターに従いコイン獲得。**獲得コイン分のカードはゲームから除外、残りは捨て札へ**（例: 6枚の畑で2金なら、2枚除外・4枚捨て札）。この除外が山札を痩せさせるのが本家仕様なので必ず再現する。
- **1枚畑の保護**: 2枚以上の畑が1つでもあるとき、1枚しか置かれていない畑は収穫できない（全畑が1枚以下なら収穫可）。0枚の畑は収穫不可。
- **植える場所がない場合**: 植える義務のある豆がどの畑にも置けないときは、先にどこかの畑を収穫して空ける（0金でも）。
- **山札が尽きたら**: 捨て札をシャッフルして山札に（ゲーム中2回まで）。**3回目に尽きた瞬間、ゲーム即終了**（めくり中でも引き中でも）。
- **ゲーム終了時**: 全員の畑を自動で収穫（豆メーター適用・保護ルール適用外）してコインに加算。手札は無価値。コインが最も多い人の勝ち。同点は同順位表示。

---

## 2) デジタル化の設計判断

### 2.1 交渉はアプリで再現しない（最重要）

交渉・値付けは**口頭**で行う。アプリ上の操作は「合意済みのカード移動」だけ:

- 手番プレイヤー: めくれた2枚それぞれを「〈プレイヤー〉にわたす」（自分も選択肢に含む＝自分で植える）。手札からも任意の位置のカードを選んで任意のプレイヤーに渡せる。
- 他プレイヤー: 自分の手札の任意の位置のカードを選んで**手番プレイヤーにだけ**渡せる。
- 渡されたカードは相手の pending に入る。手札には入らない。
- **すべての譲渡操作に確認ダイアログ**（`bbgConfirmClick` パターン）。
- 取り消し: めくれ札由来（from='deck'）の未植え pending のみ、trade フェーズ中に「もどす」で faceUp に戻せる。手札由来の譲渡は取り消し不可（確認ダイアログで防ぐ）。
- pending の植え付けは trade フェーズ中でも plantAll フェーズ中でも可能（合意即植えでテンポを上げる）。

### 2.2 フェーズ管理とトランザクション

- すべての状態遷移は `runTxn(bzRoomPath(roomId), fn)` によるルーム全体CASトランザクション（hannin と同じ流儀）。条件を満たさないときは受け取った room をそのまま返す（no-op）。
- めくり（2枚）、引き（3枚）、リシャッフル、ゲーム終了判定はトランザクション内で一括処理。
- plantAll 完了検知: pending を植えるトランザクションの末尾で「phase==='plantAll' かつ全員の pending が空」なら、同一トランザクション内で draw 3枚→手番前進→phase='plant' まで進める（誰かの端末操作を待たない）。
- 山札から n 枚取る共通処理 `bzDrawFromDeck(state, n)`: 山札が尽きたら捨て札をシャッフルして山札化し `reshuffles++`。`reshuffles` が3になったら（=3回目に尽きた）その場で `phase='result'` にして終了処理（全畑自動収穫→ranking計算）。
- **ホスト救済操作**（進行不能防止・hannin の forceAdvance 相当）:
  - plant フェーズ: 手番プレイヤーの先頭カードを自動植え（適合畑→空き畑→最大畑を収穫して植える）して trade へ進める
  - trade フェーズ: 代理で「こうかん おわり」
  - plantAll フェーズ: 残っている pending を自動植え（同上のアルゴリズム）
  - いずれも確認ダイアログ付きの「⚠ すすめる」ボタンとしてホスト画面に出す

### 2.3 画面

| screen | 役割 |
|---|---|
| `bohnanza_table` | テーブル端末（gmdev）用。ホスト以外がURL直打ちしたら `bohnanza_player` へリダイレクト（hannin_table と同じ）。全員の畑・コイン・手札枚数・山札/捨て札残・手番・めくれ札・ログを一望 |
| `bohnanza_player` | 参加者用: 自分の手札（順序固定・先頭強調）、自分の畑、フェーズに応じた操作、他プレイヤーの畑/コインのコンパクト表示 |

- プレイヤー識別: `?player=<mid>` 優先、なければ `getOrCreateLobbyMemberId(lobbyId)`（hannin と同一。デモ/テストで ?player= 差し替えが効くように必ず対応）。
- 手番が変わったら `bbgShowTurnOverlay('◯◯の ばん')`（ui のキー比較で1回だけ）。
- 畑: 豆アイコン+枚数+「いま収穫すると◯金」を常時表示。収穫ボタンは保護ルール違反時 disabled。
- 結果画面: コインランキング（メダル表示・同点同順位）。ロビー由来ならホストに「ロビーへもどる」ボタン（`setLobbyCurrentGame(lobbyId, null)` → リダイレクト。hannin の hnNextToLobby と同じ）。同室再戦機能は作らない（hannin と同等）。

### 2.4 カード画像

- 参照パス: `./assets/bohnanza/<key>.png`（key は 1.1 の8種）+ 裏面 `back.png`（任意）。`getCacheBusterParam()` の `?v=` を付ける。
- **画像が無い間はプレースホルダー描画**: 豆色の枠+絵文字+名前+総枚数+豆メーター表をCSSで描く。実装方式:
  - カードDOMは「プレースホルダー層の上に `<img>` を重ねる」構造。`load` 成功で img 表示、`error` でモジュール変数 `bzImgMissing[key]=true` を記録して img を外す。以後の再レンダーでは missing 記録済みの豆は img を出さない（スナップショット毎の404リクエスト連打を防ぐ）。
  - インライン `onerror` 属性は使わない（コードベースは addEventListener 統一）。render 後にバインドする。
  - スキャン画像を置くだけで（次回リロード時から）自動で画像表示に切り替わること。
- プレースホルダーでもゲームに必要な情報（豆種・豆メーター）が全部読めること＝「簡単な識別」要件。

### 2.5 DBスキーマ（bohnanzaRooms/<roomId>）

```js
{
  createdAt: serverNowMs(),   // ルート直下必須（cleanupOldRooms の orderByChild('createdAt') 対象）
  lobbyId: '1234',
  phase: 'plant' | 'trade' | 'plantAll' | 'result',
  order: ['mid1','mid2',...], // 席順（ロビー normalizeOrder 準拠・gmdev除く参加者のみ）
  turnIdx: 0,                 // order 内の手番位置
  turnCount: 0,               // 手番が一巡するたびでなく手番ごとに+1（表示用）
  deck: ['blue','chili',...], // 山札（先頭 = 次にめくる/引く）
  discard: ['soy',...],
  reshuffles: 0,              // 0..2。3回目の枯渇で終了
  faceUp: [ {bean:'blue', takenBy:''}, {bean:'red', takenBy:'mid2'} ] | null,  // trade中のめくれ2枚。takenBy '' = 未配布
  plantedThisTurn: 0,         // plantフェーズで植えた枚数(0-2)
  players: {
    <mid>: {
      name: 'たろう',
      hand: ['soy','red',...],     // 先頭 = 次に植える。空になるとRTDB上はキー消滅→読む側で必ず (p.hand||[])
      fields: [ {bean:'blue', count:3}, {bean:'', count:0}, ... ],  // 2〜3要素。空畑は {bean:'',count:0}（null要素はRTDBで消えるため禁止）
      coins: 0,
      pending: [ {bean:'chili', from:'deck'|'<mid>'}, ... ],  // 植える待ち
    }
  },
  result: { ranking: [ {mid, name, coins, rank}, ... ] } | null,
  log: [ 'たろうが あおまめを 2まい しゅうかく（+1金）', ... ],  // 末尾が最新・最大40件
}
```

- 手札・pending は**本人の画面にしか描画しない**（テーブル画面は枚数のみ）。DB自体は公開読み取り（既存ゲームと同じ割り切り）。
- **RTDB配列の罠**: 空配列はキーごと消える・配列要素の null は詰められる。読み出しは常に `|| []` / 空畑は `{bean:'',count:0}` で表現し null を配列に入れない。

### 2.6 バリデーション（トランザクション内で強制）

- plant: 手番プレイヤーのみ・手札先頭のみ。畑選択は「空 or 同種」のみ。2枚目は任意（スキップして trade へ）。手札0枚なら植えずに trade へ進める。
- めくり: phase==='plant' かつ plantedThisTurn>=1（手札0枚で開始した手番は0でも可）のときのみ。
- 収穫: 自分の畑・count>=1。保護ルール: 自分の畑に count>=2 の畑があるなら count===1 の畑は収穫不可。0金収穫は可（確認ダイアログに「0金です」表示）。
- 譲渡: phase==='trade' のみ。非手番→手番プレイヤーのみ。手番→任意。faceUp の配布は手番プレイヤーのみが操作。
- pending 植え: phase 'trade' か 'plantAll'。自分の pending のみ。
- 3つめの畑: order.length>=4 かつ coins>=3 かつ fields が2つ のときのみ。いつでも可。
- 全操作: phase==='result' では一切不可。

---

## 3) bbg.js 統合チェックリスト（調査済み・行番号は2026-08-18時点の目安。実装時はgrepで再確認）

| # | 内容 | 場所（bbg.js） |
|---|---|---|
| 1 | `GAME_KIND_META` に `bohnanza: { label:'ボーナンザ', emoji:'🫘', min:3 }` | 10499 |
| 2 | `renderLobbyHost` の `gameKinds` 配列に `'bohnanza'` 追加 | 10796（末尾要素の grid-column 分岐 10808 に注意） |
| 3 | ロビー内セットアップUIは**不要**（ボーナンザに事前設定なし） | — |
| 4 | `bzRoomPath` / `subscribeBohnanzaRoom` 新設 | 1149-1236 付近（hanninRoomPath 等に並べる） |
| 5 | `createBohnanzaRoom(roomId, lobbyId, members)` 新設。**ルート直下に createdAt 必須**。ロビーの order/名前から全員分の players と配札済み初期状態を一括生成（room 内 lobby フェーズは設けない） | 5856 付近の流儀 |
| 6 | 最少人数分岐に `else if (kind === 'bohnanza') min = 3;` + **最大5人チェック**（超過時トースト） | 14705-14719 |
| 7 | レガシー wordwolf ガードに `kind !== 'bohnanza'` 追加 | 14732 |
| 8 | `lobbyStartGame` に `if (kind === 'bohnanza')` ブロック（createBohnanzaRoom → setLobbyCurrentGame は共通処理） | 14882 付近 |
| 9 | 開始後の画面遷移分岐: `q.screen = isTableGm ? 'bohnanza_table' : 'bohnanza_player'; if (!isTableGm) q.player = String(mid);`（hannin の 14954 と同型） | 14929-14962 |
| 10 | `route()` に `bohnanza_table` / `bohnanza_player` 登録 | 25503 以降（hannin の 25712-25717 と同型） |
| 11 | 制限端末許可リスト `allowed` に `bohnanza_player: 1`（table は入れない） | 25585-25605 |
| 12 | `goToCurrentGame()` に `else if (kind === 'bohnanza') { q.screen='bohnanza_player'; if (isHostDevice) q.host='1'; q.player=String(mid); }` **（最重要・忘れると参加者がロビーから動けない）** | 15082-15129 |
| 13 | ヘッダー「ロビーへ戻る」の per-game 書き戻し: 不要（持ち越す設定なし。汎用処理のみで動く） | — |
| 14 | `lobbyRenderKey`: 変更不要（ロビー設定を持たないため） | — |
| 15 | `cleanupOldRooms` の `paths` に `'bohnanzaRooms'` 追加 | 1039 |
| 16 | ゲーム本体実装（カード定義/配札/トランザクション群/render/route） | 新設（hannin の構造が型: 定義→create/join→txn群→renderPlayer→routePlayer→renderTable→routeTable） |
| 17 | 購読リーク対策: `subscribe` 確立後に `ui.cancelled` なら即 unsub / popstate で unsub（hannin 7981-8010 と同じガード） | 新設コード内 |
| 18 | 古いモーダル残留対策: スナップショット毎に選択状態/モーダルの整合を検査して破棄（hannin `dropStaleModals` 7231 と同じ） | 新設コード内 |
| 19 | Firebase Console: RTDB ルールに `bohnanzaRooms` 追記（**リポジトリ外・ユーザー手作業**） | README 7.3 に手順追記 |

### 実装規約（コードベース準拠）

- `var` / 関数宣言 / 文字列連結テンプレート / `escapeHtml()` 必須（名前等の補間箇所すべて）
- イベントは `render()` 後の `addEventListener` のみ（インライン属性禁止）
- 書き込みは `setValue` / `runTxn` のみ（`.update()` ラッパーは存在しない）
- 効果音・振動は `bbgFx.*`（turn/play/reveal/win 等）、トーストは `bbgShowToast`、確認は `bbgShowConfirm`/`bbgConfirmClick`
- `shuffle` は各ゲームローカル定義が慣例（bz 内に Fisher-Yates を持つ。`randomInt` 使用）
- 時刻は `serverNowMs()`
- 他ゲームの既存コードは一切変更しない（上記チェックリストの追記のみ）
