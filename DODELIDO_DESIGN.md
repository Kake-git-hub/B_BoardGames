# ドデリド（Dodelido）実装設計書

対面プレイ補助。**コール（宣言）は口頭で行い**、アプリは「カード・3つの場・正解の判定表示・ワニの反射しょうぶ」だけを受け持つ（ボーナンザの「交渉は口頭・アプリはカード管理のみ」と同じ思想）。

- 原作: Jacques Zeimet / Drei Magier（Schmidt Spiele, 2016）・日本語版はメビウスゲームズ
- 一次情報源: 公式ルールPDF（40879_Dodelido_DE_FR_IT_GB.pdf）を調査して準拠

## 1) ゲームルール（公式準拠）

### 1.1 カード構成（全105枚）

- どうぶつ5種 × いろ5色 × 各4枚 = 100枚
  - どうぶつ: フラミンゴ🦩 / ペンギン🐧 / カメ🐢 / ラクダ🐫 / シマウマ🦓
  - いろ: しろ / ピンク / きいろ / あお / みどり
- ワニ🐊 × 5枚

### 1.2 セットアップ・進行

- 2〜6人。全カードをシャッフルして均等に配る（**あまりは箱へ＝ゲームから除外**）。各自 裏向きの山として持つ
- 場の捨て札置き場は**人数に関係なく常に3つ**。カードは**1→2→3→1…の固定ローテーション**で置く（好きな山は選べない）
- 手番の人が自分の山の一番上を1枚めくって場に置き、**めくった本人だけが即コール**する

### 1.3 コールの規則

3つの場の**一番上のカードだけ**を見る。「一致」=同じ特徴（いろ or どうぶつ）が2枚以上。

| 状況 | コール |
|---|---|
| 片方の特徴だけ一致 | その名前（「あお！」「ペンギン！」） |
| 両方一致・数がちがう | **多いほう**（きいろ3枚・フラミンゴ2枚→「きいろ！」） |
| 両方一致・同数 | **「ドデリド！」**（きいろのシマウマ3枚のような3=3も同様） |
| 何も一致しない | **「なし！」**（ラウンド1枚目も「なし」） |
| カメが見えている | 1枚につき頭に**「オー」**を付ける（「オー オー ドデリド！」）。カメがいないのに「オー」は反則 |
| ワニが出た | コールせず**全員（出した本人も）がワニのカードをたたく**。一番遅い人が場の全カードを引き取る |

### 1.4 お手つき・勝敗

- お手つき = まちがい・言いよどみ・不正な「オー」・3秒超のためらい
- お手つきした人（ワニで一番遅かった人）は**場の3山すべてを自分の山の下に加え、その本人が次のラウンドを始める**
- **最初に山を出し切った人が勝ち**（最後の1枚もコール成功が必要。ワニならたたき負けないこと）

## 2) デジタル化の設計判断

### 2.1 操作はカードの下の「タップエリア」のタップ／ながおし・答え合わせは口頭（最重要）

音声認識はせず、専用ボタンも置かない。カードの下に大きなタップエリア（`#ddTapZone`）を置き、その中のタップ/ながおしだけが効く（エリア外・ボタン上は無効。役割が合わない人のタップも無視されるので誤爆しない）。エリアの文言は「いま自分ができること」を一語だけ。

| 場面 | 操作 | だれが | エリアの文言 |
|---|---|---|---|
| めくる | タップ | 手番の人 | タップで めくる |
| せいかい → 進行 | タップ（**その1タップで自分のカードもめくれる**） | 次の人 | タップで つぎへ |
| おてつき（まちがい・言いよどみ・おそすぎ） | **ながおし**（650ms・エリアが左から赤く満ちる・はなすとキャンセル） | コールした本人 | まちがえたら ながおし |
| ワニをたたく | タップ（**pointerdownの瞬間**で計測。タップ順を下に表示） | 全員 | 🐊 たたけ！ → たたいた！ |
| ワニのけっかから再開 | タップ（そのまま1枚めくれる） | ひきとった人 | タップで さいかい |

- 正解は画面に**表示しない**（物理のドデリドと同じく、全員が場を見て口頭で判定する）。`ddComputeCall` はログへの記録にだけ使う
- 「せいかい」ボタンは無い。**次の人がタップして自分の番を始めること自体が「正解だった」の確定**（物理のドデリドと同じテンポ）
- プレイ画面の情報は最低限: **3つの場 → タップエリア → 自分ののこり枚数 → 一行の合図**（`ddLineHtml`: だれの番か／🐊ワニ！／○○がひきとり）**→ ワニのタップ順**（`ddCrocOrderHtml`: croc中ははやい順の途中経過、決着後は確定順で最遅に🐊）。ログ・ほかの人の枚数・説明文は出さない（ワニも別画面にしない）
- 誤爆よけ: めくった直後700msは次の人のタップを無視（`flippedAt`）。ワニ決着直後700msも同様（`crocResult.at`）。ボタン/リンク上のタップは対象外
- 3秒ルールはアプリでは計測せず、人間が判定する（信頼モデル）

### 2.2 ワニだけはアプリが反射しょうぶを計測する

全員が**自分の端末のがめんをたたく**（どこでもよい）。サーバー時刻基準（`serverNowMs()`）で `croc.startAt` からの経過msを端末側で計算して記録し、
全員たたいたら自動で決着（一番遅い人が全引き取り）。端末が動かない人がいるときは「うちきる」ボタンでたたいていない人を引き取り役にできる。

### 2.3 フェーズ管理

```
flip（手番の人のタップで1枚めくる）
  ├─ ふつうのカード → call（口頭コール。正解は本人以外の画面に表示）
  │      ├─ 次の人のタップ: 前の人の山が0枚なら → result（勝ち）／ 残っていれば手番前進+そのまま次の人の1枚がめくれて call
  │      └─ 本人のながおし: 場の全カードを引き取り、同じ人の flip（新ラウンド）
  └─ ワニ → croc（全員タップ）→ crocResult（順位表示。ひきとった人のタップで再開+そのまま1枚めくれて call）
                └─ めくった本人が最後の1枚で、引き取り役でなければ → result（勝ち）
```

すべての書き込みは `ddTxn`（bohnanza の `bzTxn` と同型のCASトランザクション）経由。`phase==='result'` 後は一切変更不可。
タップ/ながおしのリスナーは `#view` に張るため、`ui.detachTap`（redirectToLobby / popstate）で必ず解除する。

### 2.4 画面

- `dodelido_player` … 参加者画面（ロビーの全員が自動参加）。1カラム中央ぞろえで **タイトル行（左〜中央=番手 / 右はし=音のON/OFF）→ 3つの場 → 正方形のタップエリア（一辺は幅いっぱい or 画面高さの36%〈タブレット34%〉の小さいほう）→ のこり枚数（大）→ ワニのタップ順** だけ（`.dd-simple`）。**ワニ中（croc / crocResult）は のこり枚数を出さない**（6人分のタップ順が画面に収まるように）。ワニ中だけタップ順の行末に小さな「うちきる」
- `dodelido_table` … テーブル端末（`gmdev=1`・ホストのみ）。タイトル行（番手＋音）＋3つの場＋ワニのタップ順＋席（名前と枚数）。操作は「⚠すすめる（だいり）」（`ddHostForce`）と「ロビーへもどる」だけ
- **番手のモーダル**（ラウンドが変わった瞬間の「○○の ばん」）は画面ぜんたいを覆う共通オーバーレイ（`bbgShowTurnOverlay`）ではなく、**場のカードに重ねる専用の `ddShowTurnOverlay`**（`.dd-turnov`・1.2秒・`pointer-events:none`）。タップエリアを隠さないため
- けっか画面のランキングは bohnanza の共用HUDクラス（`.bz-rank` 等）を再利用し、ゲーム固有部分は `dd-` プレフィックス

### 2.5 カード画像

`./assets/dodelido/<いろ>_<どうぶつ>.png`（例 `blue_penguin.png`）と `croc.png` を置くだけでプレースホルダー（色わく＋絵文字＋名前＋色名）から自動で切り替わる（bohnanza と同じ `ddImgMissing` 方式）。

### 2.6 DBスキーマ（dodelidoRooms/<roomId>）

```
{
  createdAt, lobbyId,
  phase: 'flip' | 'call' | 'croc' | 'crocResult' | 'result',
  order: [mid...],
  turnIdx,                 // いまめくる人（order の添字）
  turnCount,               // ラウンド通し番号（flip に戻るたびに +1。オーバーレイ/デモのキー）
  pileIdx,                 // つぎに置く場（0-2 の固定ローテーション）
  piles: [[key...]x3],     // 各山 下→上。カードは 'いろ:どうぶつ' 文字列、ワニは 'croc'
  flippedAt,               // 直近のめくり時刻（次の人のタップの誤爆よけ）
  croc: { startAt, taps: {mid: 経過ms} } | null,
  crocResult: { loserMid, closed, at, ranking:[{mid,ms,tapped}] } | null,
  players: { mid: { name, deck: [key...] } },   // deck[0]=一番上。引き取りは末尾（山の下）へ
  result: { winnerMid, ranking:[{mid,name,cards,rank}] } | null,
  log: [...]               // 最大40行
}
```

- RTDBの空配列消失対策: `piles` はオブジェクト形で返ることもあるため `ddNormPiles` が常に3本の配列へ正規化
- 正解は保存しない（`ddComputeCall(piles)` の純関数で毎回計算。全端末で同じ結果になる）

### 2.7 バリデーション（トランザクション内で強制）

- flip: 手番本人のみ・山が1枚以上
- call: 進行（`ddTapNext`）は**次の人**のみ、おてつき（`ddJudgeMiss`）は**コールした本人**のみ
- croc: 参加者のみ・二重タップ不可。全員タップで自動決着。再開（`ddCrocResume`）は**ひきとった本人**のみ
- result 後は全操作 no-op

## 3) bbg.js 統合ポイント

1. `cleanupOldRooms()` の paths に `dodelidoRooms`
2. `GAME_KIND_META` / `gameKinds` に `dodelido`（🦩・min 2）
3. ロビー開始: 人数ゲート 2〜6人 → `createDodelidoRoom(roomId, lobbyId, members)`（配札まで一括）→ `dodelido_player` / `dodelido_table` へ
4. `routeLobbyPlayer.goToCurrentGame` に dodelido 分岐（`player=<mid>`）
5. 制限端末の allowed に `dodelido_player` / `demo_dodelido`
6. ルーティング: `dodelido_table`（非ホストは player へ回す）/ `dodelido_player` / `demo_dodelido`
7. Firebase Console で RTDBルールに `dodelidoRooms` の追記が必要（README §7.3）

## 4) デモ自動プレイモード（demo_dodelido）

既存のデモ枠組み（`demoState`/`demoTick`/plan方式）に `'dd'` として追加。ボット4人。

- flip → 手番ボットがタップでめくる（0.7〜1.4s）
- call → 口頭コールぶんの間（1.1〜2.1s）→ **15%で本人がながおしひきとり**、それ以外は次のボットがタップ（`ddTapNext`）
- croc → ハッシュから疑似反応時間（350〜1750ms）を作り、はやい順に1人ずつタップ。全員たたくと本番側が自動決着
- crocResult → 1.4〜2.4s 見せて ひきとったボットがタップで再開（`ddCrocResume`）
- スタック時の代理: key が `dd_` 始まりなら `ddHostForce` が全フェーズを進められる（bz と同様）
- 決着判定: `phase === 'result'`（10秒後に新しいデモを自動再開）

## 5) レイアウト（ノースクロール規約準拠）

- 縦積み基本、幅768以上は `.bbg-wide` + `.bbg-2col` の2カラム
- カード高さは幅768以上で `clamp(110px, 17dvh, 168px)`（vhフォールバック併記）
- ログは `.bz-log`（上限つき内部スクロール・コンパクトモード連動）を再利用
- 検証: 1024×768 / 768×1024 で `scrollHeight - innerHeight <= 0`、既存画面の回帰なし
