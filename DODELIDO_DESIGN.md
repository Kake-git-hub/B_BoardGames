# ドデリド（Dodelido）実装設計書

対面プレイ補助。**コール（宣言）は口頭で行い**、アプリは「カード・3つの場・正解の判定表示・ワニの反射しょうぶ」だけを受け持つ（ボーナンザの「交渉は口頭・アプリはカード管理のみ」と同じ思想）。

- 原作: Jacques Zeimet / Drei Magier（Schmidt Spiele, 2016）・日本語版はメビウスゲームズ
- 一次情報源: 公式ルールPDF（40879_Dodelido_DE_FR_IT_GB.pdf）を調査して準拠

## 1) ゲームルール（公式準拠）

### 1.1 カード構成（全105枚）

- どうぶつ5種 × いろ5色 × 各4枚 = 100枚
  - どうぶつ: フラミンゴ🦩 / ペンギン🐧 / カメ🐢 / ラクダ🐫 / ナマケモノ🦥
  - いろ: しろ / ピンク / きいろ / あお / みどり
- ワニ🐊 × 5枚（絵は5まいとも別。アプリではロビー設定で 0〜10枚に変更できる）

### 1.2 セットアップ・進行

- 2〜6人。全カードをシャッフルして均等に配る（**あまりは箱へ＝ゲームから除外**）。各自 裏向きの山として持つ（アプリではロビー設定で さいしょの手札枚数を へらせる → §2.5.1）
- 場の捨て札置き場は**人数に関係なく常に3つ**。カードは**1→2→3→1…の固定ローテーション**で置く（好きな山は選べない）
- 手番の人が自分の山の一番上を1枚めくって場に置き、**めくった本人だけが即コール**する

### 1.3 コールの規則

3つの場の**一番上のカードだけ**を見る。「一致」=同じ特徴（いろ or どうぶつ）が2枚以上。

| 状況 | コール |
|---|---|
| 片方の特徴だけ一致 | その名前（「あお！」「ペンギン！」） |
| 両方一致・数がちがう | **多いほう**（きいろ3枚・フラミンゴ2枚→「きいろ！」） |
| 両方一致・同数 | **「ドデリド！」**（きいろのナマケモノ3枚のような3=3も同様） |
| 何も一致しない | **「なし！」**（ラウンド1枚目も「なし」） |
| カメが見えている | 1枚につき頭に**「オー」**を付ける（「オー オー ドデリド！」）。カメがいないのに「オー」は反則 |
| ワニが出た | コールせず**全員（出した本人も）がワニのカードをたたく**。一番遅い人が場の全カードを引き取る |

### 1.4 お手つき・勝敗

- お手つき = まちがい・言いよどみ・不正な「オー」・3秒超のためらい
- お手つきした人（ワニで一番遅かった人）は**場の3山すべてを自分の山の下に加え、その本人が次のラウンドを始める**
- **最初に山を出し切った人が勝ち**（最後の1枚もコール成功が必要。ワニならたたき負けないこと）

## 2) デジタル化の設計判断

### 2.1 操作はカードの下の「タップエリア」のタップ／ながおし・答え合わせは口頭（最重要）

音声認識はせず、専用ボタンも置かない。カードの下に大きなタップエリア（`#ddTapZone`）を置き、その中のタップ/ながおしだけが効く（エリア外・ボタン上は無効。役割が合わない人のタップも無視されるので誤爆しない）。エリアの文言は「いま自分ができること」を一語だけ。

| 場面 | 操作 | だれが | エリアの文言 |
|---|---|---|---|
| めくる | タップ | 手番の人 | めくる！（大きく） |
| せいかい → 進行 | タップ（**その1タップで自分のカードもめくれる**） | 次の人 | めくる！（大きく） |
| おてつき（まちがい・言いよどみ・おそすぎ） | **ながおし**（650ms・エリアが左から赤く満ちる・はなすとキャンセル） | **コール中の「めくる！」の人いがいのだれでも**（ながおしした本人がひきとる。まえのラウンドのまちがいに あとから気づいた人の遡りひきとりもこれ） | 本人: まちがえたら ながおし ／ ほかの人: まちがいに きづいたら ながおし |
| ワニをたたく | タップ（**pointerdownの瞬間**で計測。タップ順を下に表示） | 全員 | 🐊 たたけ！ → たたいた！ |
| ワニのけっかから再開 | タップ（そのまま1枚めくれる） | ひきとった人 | めくる！（大きく） |

- **番手は `ddActorMid`（＝つぎに タップして めくる人）で出す**。`turnIdx` は「さいごにめくった人」なので、コール中（`call`）はそのつぎの人が番手。これを使わないと「タップしてから じぶんの番と出る」=1つズレて見える。番手の一行（`ddLineHtml`）・スマホ横置きの順番リスト（`ddSeatsHtml`）・番手ポップアップ・テーブルの席ハイライトは すべてこれで統一する
- **「めくる！」の人には ながおしを付けない**（`onDdPointerDown` が `ddActorMid` と一致する人の hold timer を張らない）。勢いよく押しっぱなしにしても おてつきにならない。あわせて、ながおしが無い人（`ptr.holdArmed === false`）は**押している長さでタップを捨てない**（従来の「600ms超は無視」は ながおし持ちだけに効かせる）
- **指をおろした時とフェーズが変わっていたら、はなす動作は操作にしない**（`ptr.phase` を覚えて `ddHandleTap(fromPhase)` で照合）。ワニをたたいた指は `ptr.consumed` で使いきりにする。これが無いと**ワニをたたいた指をはなした瞬間に「さいかい」してしまい、タップする前に1まいめくれてしまう**（`crocResult.at` の700ms誤爆よけだけでは、指を長くのせていると すりぬける）
- **一拍（`DD_BEAT_MS` = 380ms）＋ 配られるアニメ（`.dd-card--drop` = 0.62s）**: タップした瞬間ではなく、ひと呼吸おいてから書きこむ（`ddBeatThen`）。**間そのものは短く、カードが落ちる動きのほうを ゆっくりにして拍を取る**（間が長いと「止まった」と感じる）。タップ直後は音（`bbgFx.tap`）とタップ枠の光（`.dd-tapzone--beat` = 0.38s）で受付を伝える。カードが出るときの音（`bbgFx.reveal`）は再描画側で鳴るので、一拍のあとに揃う。`ui.inFlight` はタップの瞬間から立てるので待ち中の連打は無視。ワニをたたく（`ddSlapNow`）とながおしには一拍を入れない
- 正解は画面に**表示しない**（物理のドデリドと同じく、全員が場を見て口頭で判定する）。`ddComputeCall` はログへの記録にだけ使う
- 「せいかい」ボタンは無い。**次の人がタップして自分の番を始めること自体が「正解だった」の確定**（物理のドデリドと同じテンポ）
- プレイ画面の情報は最低限: **3つの場 → タップエリア → 自分ののこり枚数 → 一行の合図**（`ddLineHtml`: だれの番か／🐊ワニ！／○○がひきとり）**→ ワニのタップ順**（`ddCrocOrderHtml`: croc中ははやい順の途中経過、決着後は確定順で最遅に🐊）。ログ・ほかの人の枚数・説明文は出さない（ワニも別画面にしない）
- 誤爆よけ: めくった直後700msは次の人のタップを無視（`flippedAt`）。ワニ決着直後700msも同様（`crocResult.at`）。ボタン/リンク上のタップは対象外
- 3秒ルールはアプリでは計測せず、人間が判定する（信頼モデル）

### 2.2 ワニだけはアプリが反射しょうぶを計測する

全員が**自分の端末のがめんをたたく**（どこでもよい）。サーバー時刻基準（`serverNowMs()`）で `croc.startAt` からの経過msを端末側で計算して記録し、
全員たたいたら自動で決着（一番遅い人が全引き取り）。端末が動かない人がいるときは「うちきる」ボタンでたたいていない人を引き取り役にできる。

### 2.3 フェーズ管理

```
flip（手番の人のタップで1枚めくる）
  ├─ ふつうのカード → call（口頭コール。正解は本人以外の画面に表示）
  │      ├─ 次の人のタップ: 前の人の山が0枚なら → result（勝ち）／ 残っていれば手番前進+そのまま次の人の1枚がめくれて call
  │      └─ だれかのながおし: ながおしした本人が場の全カードを引き取り、その人の flip（新ラウンド）
  │         （コール本人のおてつきも、まえのまちがいに気づいた人の遡りひきとりも同じ動き。場が空なら何もしない）
  └─ ワニ → croc（全員タップ）→ crocResult（順位表示。ひきとった人のタップで再開+そのまま1枚めくれて call）
                └─ めくった本人が最後の1枚で、引き取り役でなければ → result（勝ち）
```

すべての書き込みは `ddTxn`（bohnanza の `bzTxn` と同型のCASトランザクション）経由。`phase==='result'` 後は一切変更不可。
タップ/ながおしのリスナーは `#view` に張るため、`ui.detachTap`（redirectToLobby / popstate）で必ず解除する。

### 2.4 画面

- `dodelido_player` … 参加者画面（ロビーの全員が自動参加）。たて画面は1カラム中央ぞろえで **タイトル行（番手）→ 3つの場 → 正方形のタップエリア（一辺は幅いっぱい or 画面高さの36%〈タブレット34%〉の小さいほう）→ のこり枚数（大）→ ワニのタップ順**（`.dd-simple`。順番リスト `.dd-seats` はたてでは非表示）。**ワニ中（croc / crocResult）は のこり枚数を出さない**（6人分のタップ順が画面に収まるように）。ワニ中だけタップ順の行末に小さな「うちきる」
- `dodelido_table` … テーブル端末（`gmdev=1`・ホストのみ）。タイトル行（番手＋音）＋3つの場＋ワニのタップ順＋席（名前と枚数）。操作は「⚠すすめる（だいり）」（`ddHostForce`）と「ロビーへもどる」だけ
- **番手のポップアップ**（ラウンドが変わった瞬間の「○○の ばん」）は画面ぜんたいを覆う共通オーバーレイ（`bbgShowTurnOverlay`）を使わず、**タイトル行の番手テキストの位置**（＝カードより上）に出す専用の `ddShowTurnOverlay`（`.dd-line` の中へ `.dd-turnov` を挿入・1.2秒・`pointer-events:none`）。**カードにもタップエリアにも重ねない**（場のカードを隠さないため）。行の高さは `min-height:1.9em` で固定なので、出ても画面レイアウトは動かない
- けっか画面のランキングは bohnanza の共用HUDクラス（`.bz-rank` 等）を再利用し、ゲーム固有部分は `dd-` プレフィックス
- **スマホ横置き**（media query `(orientation:landscape) and (max-height:520px)` + `:has()`）: プレイヤー画面をグリッド**よこ1列**に組み替え、**左=全プレイヤーの順番リスト（`.dd-seats`・番手をハイライト）／中=3つの場（上下いっぱい）／右=タップ枠（たて書きで「めくる！」）**。**ワニ中は この順番リストが そのままタップ順の表示になる**（`.dd-seats--croc`: はやい順に並べかえ、1行を2段組みにして 順位・名前・秒数を出し、いちばん下のビリを赤＋「🐊ビリ」で示す。重なる `.dd-ord` チップは横置きでは出さず、`.dd-order` は「うちきる」ボタンの置き場としてだけ残す）。**タイトルバーの ⇄（`#ddSwapBtn`）で左右をいれかえられる**（ききうで合わせ・`bbg_dd_swap_v1` に端末保存 → `.dd-simple.dd-swap`。列はばも `--dd-seatw`/`--dd-tapw` ごと入れかわるのでカードの大きさは変わらない）。番手の一行（`.dd-head`）と画面内の のこり枚数（`.dd-count`）は出さず、**のこり枚数はタイトルバー右のスロット（`#bbgHeaderExtra` / `bbgSetHeaderExtra`）へ**（route() のあたまで毎回クリアする）。**ヘッダーは ふだん プレイ画面では隠される**（`updateHeaderLobbyBackButton` の `isGameScreen`）ので、`dodelido_player` だけ例外にして `.header--dd` を付けて出している（`.header--dd` は CSS でたて画面のときだけ隠す）。左右の列は clamp の固定はばなので、ゲームが進んでもカードの位置は動かない。自分の番でなくてもタップ枠は消さない（消すと中の列の幅が動く）。カードの大きさは `container-type:size` が使えるなら場の枠の実寸（`cqw`/`cqh`）から、使えなければ `--dd-hchrome`/`--dd-vchrome` の実測値から出す。アプリの幅520上限を外し footer を隠す。**タップ/ながおしは画面ぜんたい（カードエリア含む）で有効**（JS側 `ddPhoneLandscape()` が同条件の matchMedia を見る。**CSSとJSの条件は必ず一致させる**）。ながおしの赤満ちはカードエリアにも出す。ワニのタップ順はカード下端に重ねる。テーブル画面（`.bz-table`）と縦持ちは従来のまま。デモバーも同条件で1行コンパクト化（`body.bbg-demo-on .app` の下余白42px）
  - **カードの大きさは「幅」を先に決める**（`width:min((100vw - var(--dd-hchrome))/3, (100dvh - var(--dd-vchrome))*.75)`・高さは `aspect-ratio:3/4` が出す）。`height:100%`＋`aspect-ratio` で幅を導くと、**Safari/Firefox は `.dd-pile` の intrinsic width をプレースホルダーの文字ぶんしか取らない**（高さを不定として扱うため）ので山が細く並び、カードだけが本来の幅で描かれて**3枚が重なる**（Chromeでは再現しない）。`--dd-hchrome:100px` / `--dd-vchrome:115px`（デモバー表示中は151px）はカード以外に使う余白の実測合計

### 2.5 カード画像

`./assets/dodelido/<いろ>_<どうぶつ>.png`（例 `blue_penguin.png`）と `croc1.png`〜`croc5.png`（ワニは絵ちがい5まい。デッキのキーも `croc1`〜`croc5`）を置くだけでプレースホルダー（色わく＋絵文字＋名前＋色名）から自動で切り替わる（bohnanza と同じ `ddImgMissing` 方式）。

**先読み（チラつき防止）**: `<img>` は再描画のたびに作りなおされるので、load を待って `is-loaded` を付ける方式だと**配られた瞬間に枠だけ見える**。そこで `ddPreloadCardImages()` で全30キー（25組合せ＋croc1〜5）を先に読み、読み込みずみ（`ddImgReady`）のカードは**HTML生成の時点で `is-loaded` を付けて出す**（＋`decoding="sync"`）。呼ぶ場所は ロビー（ドデリド選択時／参加者側は currentGame・lastKind が dodelido のとき）と `routeDodelidoPlayer` / `routeDodelidoTable` の入口。`.dd-card-img` の背景は透明（万一まだ描けていないフレームでも、下のプレースホルダーが見えるだけで済む）。

### 2.5.1 ロビー設定（`lobbies/<id>/dodelidoSettings`）

ホストのロビー画面（ドデリドを選んでいるときだけ表示）で2つ設定できる。`oekakiSettings` と同じパターン（`normalizeDodelidoLobbySettings` / `setLobbyDodelidoSettings` / `lobbyRenderKey` に追加）。

| キー | 意味 | 値 | きほん |
|---|---|---|---|
| `handCount` | ひとりの さいしょの てふだ枚数 | `0`（じどう＝山を人数でわけきる）/ 5 / 8 / 10 / 12 / 15 / 20 / 25 | `0` |
| `crocCount` | ワニの まいすう | 0〜10 | `5` |

- `handCount` が配れる枚数より多いときは、配れるところまで（`Math.min(handCount, floor(deck/人数)))`）。あまったカードは これまでどおり はこへ
- `crocCount` は `ddBuildDeck(crocCount)` に渡る。6まい以上にすると絵は `croc1`〜`croc5` をくりかえす
- 設定は開始時のログ行（`ゲームかいし！ ひとり Nまいずつ（ワニ Mまい・あまり…）`）にも出る

### 2.6 DBスキーマ（dodelidoRooms/<roomId>）

```
{
  createdAt, lobbyId,
  phase: 'flip' | 'call' | 'croc' | 'crocResult' | 'result',
  order: [mid...],
  turnIdx,                 // いまめくる人（order の添字）
  turnCount,               // ラウンド通し番号（flip に戻るたびに +1。オーバーレイ/デモのキー）
  pileIdx,                 // つぎに置く場（0-2 の固定ローテーション）
  piles: [[key...]x3],     // 各山 下→上。カードは 'いろ:どうぶつ' 文字列、ワニは 'croc'
  flippedAt,               // 直近のめくり時刻（次の人のタップの誤爆よけ）
  croc: { startAt, taps: {mid: 経過ms} } | null,
  crocResult: { loserMid, closed, at, ranking:[{mid,ms,tapped}] } | null,
  players: { mid: { name, deck: [key...] } },   // deck[0]=一番上。引き取りは末尾（山の下）へ
  result: { winnerMid, ranking:[{mid,name,cards,rank}] } | null,
  log: [...]               // 最大40行
}
```

- RTDBの空配列消失対策: `piles` はオブジェクト形で返ることもあるため `ddNormPiles` が常に3本の配列へ正規化
- 正解は保存しない（`ddComputeCall(piles)` の純関数で毎回計算。全端末で同じ結果になる）

### 2.7 バリデーション（トランザクション内で強制）

- flip: 手番本人のみ・山が1枚以上
- call: 進行（`ddTapNext`）は**次の人**のみ、おてつき（`ddJudgeMiss`）は**orderにいる人ならだれでも**（ながおしした本人がひきとり・場が空なら no-op・その人の番から再開）
- croc: 参加者のみ・二重タップ不可。全員タップで自動決着。再開（`ddCrocResume`）は**ひきとった本人**のみ
- result 後は全操作 no-op

## 3) bbg.js 統合ポイント

1. `cleanupOldRooms()` の paths に `dodelidoRooms`
2. `GAME_KIND_META` / `gameKinds` に `dodelido`（🦩・min 2）
3. ロビー開始: 人数ゲート 2〜6人 → `createDodelidoRoom(roomId, lobbyId, members, settings)`（配札まで一括）→ `dodelido_player` / `dodelido_table` へ
4. `routeLobbyPlayer.goToCurrentGame` に dodelido 分岐（`player=<mid>`）
5. 制限端末の allowed に `dodelido_player` / `demo_dodelido`
6. ルーティング: `dodelido_table`（非ホストは player へ回す）/ `dodelido_player` / `demo_dodelido`
7. Firebase Console で RTDBルールに `dodelidoRooms` の追記が必要（README §7.3）

## 4) デモ自動プレイモード（demo_dodelido）

既存のデモ枠組み（`demoState`/`demoTick`/plan方式）に `'dd'` として追加。ボット4人。

- flip → 手番ボットがタップでめくる（0.7〜1.4s）
- call → 口頭コールぶんの間（1.1〜2.1s）→ **15%で本人がながおしひきとり／7%でまえのボットが遡りひきとり**、それ以外は次のボットがタップ（`ddTapNext`）
- croc → ハッシュから疑似反応時間（350〜1750ms）を作り、はやい順に1人ずつタップ。全員たたくと本番側が自動決着
- crocResult → 1.4〜2.4s 見せて ひきとったボットがタップで再開（`ddCrocResume`）
- スタック時の代理: key が `dd_` 始まりなら `ddHostForce` が全フェーズを進められる（bz と同様）
- 決着判定: `phase === 'result'`（10秒後に新しいデモを自動再開）

## 5) レイアウト（ノースクロール規約準拠）

- 縦積み基本、幅768以上は `.bbg-wide` + `.bbg-2col` の2カラム
- カード高さは幅768以上で `clamp(110px, 17dvh, 168px)`（vhフォールバック併記）
- ログは `.bz-log`（上限つき内部スクロール・コンパクトモード連動）を再利用
- 検証: 1024×768 / 768×1024 で `scrollHeight - innerHeight <= 0`、既存画面の回帰なし
